#include <iostream>
#include <string>
using namespace std;

int main(void) {
    int c;
    cin >> c;
    
    while(c--) {
        string members, fans;
        cin >> members >> fens;
        int membersCnt = members.length();
        int fensCnt = members.length();
        
    }
}